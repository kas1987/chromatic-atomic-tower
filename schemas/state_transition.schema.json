{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "CAT State Transition Rule",
  "description": "A single allowed state transition with preconditions.",
  "type": "object",
  "required": ["from", "to"],
  "properties": {
    "from": {
      "type": "string",
      "description": "Source state"
    },
    "to": {
      "type": "string",
      "description": "Target state"
    },
    "guard": {
      "type": "string",
      "description": "Guard condition that must be satisfied",
      "default": "none"
    },
    "reversible": {
      "type": "boolean",
      "description": "Whether this transition can be undone",
      "default": false
    },
    "requires": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Preconditions that must hold before transition is allowed"
    },
    "forbidden_when": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Conditions that block this transition"
    }
  }
}
