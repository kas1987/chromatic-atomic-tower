# Reference

This folder stores non-operational source material.

The M1-M4 Mission Packet reference image is included as background only. The operational CAT contracts live in YAML and JSON.

ID migration support:

- `ID_CROSSWALK_TEMPLATE.md` provides a mapping format from legacy IDs to new mission/bead IDs and routing hints.
