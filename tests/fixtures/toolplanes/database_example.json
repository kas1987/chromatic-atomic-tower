{
  "plane": "database",
  "engines": ["sqlite", "duckdb"],
  "capabilities": ["query", "read"],
  "default_mode": "read_only",
  "connection_ref": "loghouse_sqlite",
  "allowed_databases": ["evidence", "codegraph"],
  "forbidden_operations": ["DROP", "TRUNCATE"]
}
