# Model Usage Notes

CAT treats model choice as routing, not governance.

The mission and BEAD define authority. The model performs a role inside that authority.

Use stronger reasoning models for:

- architecture
- governance
- critical review
- security
- state transition design

Use faster models for:

- simple docs
- low-risk formatting
- basic extraction
- repeated validation summaries

Never allow model capability to override mission gates.
